### 0.1.8
* Added `Fuzzr.FromEach`.

### 0.1.9
* Added `.ToList()`.
* Added `.ToArray()`.
* Added `Fuzzr.Tuple(...)`.
* Removed the `Func<T, T>` overload of `.Apply(...)`.  
  Use LINQ `Select` instead.